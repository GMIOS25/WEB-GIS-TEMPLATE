# Thay bằng domain thật khi có (xem docs/en/DEPLOYMENT & FLEET STRATEGY.md Section 4.2).
# Caddy tự lo ACME/Let's Encrypt - không cần bước certbot thủ công.
gis.gialai.gov.vn {
    encode zstd gzip
    reverse_proxy app:8080
}
